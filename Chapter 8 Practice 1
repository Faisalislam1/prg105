print("This program accepts a phrase and returns the acronym.")
string = str(input(("Please enter a phrase to convert: ")))
word_list = string.split()
first_letter = ""
counter = 0
for i in word_list:

    first_letter += i[0]
print(f"{first_letter.upper()}")
